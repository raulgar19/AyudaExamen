@model List<Comic>

<h1>Comics</h1>

<table class="table table-warning">
    <thead>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Year</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (Comic comic in Model)
        {
            <tr>
                <td>@comic.Name</td>
                <td>@comic.Author</td>
                <td>@comic.Year</td>
                <td>
                    <a asp-controller="Comics" asp-action="Details" asp-route-id="@comic.Id" class="btn btn-info">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>